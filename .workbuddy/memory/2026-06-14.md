# 2026-06-14 工作日志

## 简历评估报告 UI Bug 修复（3个问题）

### 问题1："通过"状态颜色不一致
- **根因**：`resumeAudit.ts` 的 `toAuditCheck()` 保留原始 `severity` 字段，前端 `App.tsx` 在渲染时对 passed=true 的项仍添加 `.error`/`.warning` CSS class。CSS 中 `.audit-status.error` 定义在 `.audit-status.passed` 之后，导致 severity=error 的通过项显示红色，severity=warning 的通过项显示绿色。
- **修复**：`App.tsx` 中两处渲染逻辑（audit-meter-segment 和 audit-status），改为 `check.passed ? 不加severity class : 加上severity class`

### 问题2：技能列表错误包含邮箱和用户名
- **根因**：`buildSkillCheck()` 用宽泛正则 `/[A-Za-z][A-Za-z0-9+#. -]{1,30}/` 匹配技术词汇，过滤条件仅要求包含字母，导致 `manxinhuanxi123`（用户名）和 `foxmail.com`（邮箱域名）被当作技能。
- **修复**：新增 `isNonSkillTerm()` 过滤函数 + `NON_SKILL_PATTERNS` 正则列表，过滤 email(@)、URL、域名、纯数字字母用户名等非技能内容。

### 问题3：JD 解读卡片布局 + 加分项出现"1""2"空序号
- **布局**：`.jd-interpretation-grid` 从固定2列 grid 改为 CSS `column-count: 2` 瀑布流布局（类似小红书），卡片高度自适应不再强制对齐。
- **加分项**：`tencentMatchBuilder.ts` 的 `splitText()` 新增过滤规则 `/^\d{1,2}[\.\、\s]*$/`，排除纯数字序号行（如 "1"、"2"、"1."）。

### 问题4：每个岗位都显示"5 个待补关键词"
- **根因**：`extractKeywords()` 正则 `[\u4e00-\u9fa5]{2,8}` 太宽泛（2字中文即命中），停用词仅 9 个 → 几乎每个岗位都能提取出 ≥5 个通用词。
- **修复**：
  - 中文关键词要求至少 3 个字（减少虚词干扰）
  - 停用词从 9 个扩展到 ~50 个（含能力/经验/熟悉/掌握/参与等通用描述词、城市名、学历学位等）
  - 英文术语和中文分开展开再合并，避免混合截断不均

### 问题5：筛选"校招"显示 0 个岗位
- **根因**：`buildFilteredShortlist()` 做硬过滤——腾讯返回的 5 个岗位 type 都含"实习"，选"校招"后全部被过滤掉，结果为空。
- **修复**：增加回退逻辑——当筛选结果为 0 时，自动回退显示全部岗位。宁可硬性条件待补多一点，也不要出现"暂无岗位"。

## 结果页修改 (22:59)

1. **修复"需要优先改"卡片溢出背景**: 
   - 第一次：在 `.resume-diagnostic-panel, .resume-action-panel` 添加了 `overflow: hidden`（不够）
   - 第二次（23:02）：在 `.resume-workbench-grid` 添加 `overflow: hidden`；在 `.report-block` 和 `.audit-priority-issues` 添加 `overflow-wrap: break-word; word-break: break-word;`
   - 第三次（23:03）：`overflow: hidden` 导致内容被裁剪——**回退所有 `overflow: hidden`**，改以**布局调整**解决：grid 从 `minmax(280px, 0.4fr) minmax(360px, 0.6fr)` 改为 `1fr 1fr` 等宽两栏，保留 `word-break: break-word` 防止长文本撑破

### 第四次（23:10）：完整排版重构
用户要求将结果页改为自上而下的卡片流布局：
- **评分卡片**：从左栏内移到工作台顶部，全宽展示分数 + verdict
- **两栏中段**：左 = 检查明细（仅"不足"+"建议改进"），右 = 立即修改（优先问题）
- **全宽底部分**：岗位定制 → 改写示例 → 下一步动作，每个占满整行
- **诚信声明**：移到底部全宽
- 实现方式：重写 `ResumeOptimizationWorkbench`，删除 `ResumeDiagnosticPanel` 和 `ResumeActionPanel`（数据计算和渲染直接内联到父组件）
- grid 恢复为 `minmax(280px, 0.4fr) minmax(360px, 0.6fr)`（卡片保持原宽度）
- `.resume-workbench` 是单列 grid，直接子元素自动全宽

### 第五次（23:13）：检查明细从表格改为卡片列表
紧凑模式下的三列表格在窄面板中观感差（说明文字逐字换行），改为纵向卡片布局：
- `AuditCheckTable` 紧凑模式改为卡片列表（`audit-check-card`），每张卡片 = 状态标签 + 检查项名 → 说明文字
- 删除旧 `.audit-check-table-block.compact` 相关 CSS（包括移动端规则）
- 新增 `.audit-check-card-list` / `.audit-check-card` / `.audit-check-card-head` 样式
2. **删除左栏"检查明细"**: 从 `ResumeDiagnosticPanel` 组件中移除 `<AuditCheckTable>`
3. **删除左栏"亮点可保留"**: 从 `ResumeDiagnosticPanel` 组件中移除两处 `<AuditList title="亮点可保留">`（audit 分支和非 audit 分支），并清理了未使用的 `highlights` 变量

### 第六次（23:15）：修复卡片溢出背景
- 在 `.resume-diagnostic-panel, .resume-action-panel` 重新添加 `overflow: hidden`
- 之前移除是因为当时面板内容过多导致裁剪，现在布局已重组（评分全宽、底部全宽），`overflow: hidden` 仅阻止视觉溢出，不会裁剪正常内容
- TypeScript 编译通过
